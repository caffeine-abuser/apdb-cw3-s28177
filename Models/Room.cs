public class Room(int id, string name, string buildingCode, int floor, int capacity, bool hasProjector, bool isActive)
{
    int Id = id;
    string Name = name;
    string BuildingCode = buildingCode;
    int Floor = floor;
    int Capacity = capacity;
    bool HasProjector = hasProjector;
    bool IsActive = isActive;
}